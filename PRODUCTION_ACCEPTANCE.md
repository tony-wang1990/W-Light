# W-Light 生产上线验收清单

本清单用于把项目从“可内测”推进到“可生产使用”。每完成一项，需要同步更新状态，后续继续开发时优先从未完成项往下推进。

当前阶段：**内测验收与生产加固**

状态说明：

- `[x]` 已完成并已验证。
- `[~]` 已实现基础能力，但还需要真实环境验收或补强。
- `[ ]` 未完成或尚未验证。

## 1. 服务器部署与基础运维

- [x] 支持 Docker Compose 部署 Web/API/PostgreSQL/Redis/MinIO。
- [x] 支持默认 `3005` Web 端口。
- [x] 支持 ARM64 和 AMD64 服务器部署。
- [x] 支持 1C1G 小服务器自动创建 swap。
- [x] 提供 `scripts/server-deploy.sh` 一键部署/更新脚本。
- [x] 提供 `scripts/server-admin.sh` 管理员创建/重置脚本。
- [x] 提供 `scripts/server-backup.sh` PostgreSQL + MinIO + `.env` 备份恢复脚本，支持列表、校验、恢复确认和旧备份清理。
- [~] 服务器备份脚本已加固，仍需在真实生产机执行一次备份、校验、恢复和回滚演练。
- [~] 定时备份安装/移除入口已具备，仍需在真实生产机执行 `scripts/server-backup.sh --install-cron` 并确认 `crontab -l`。
- [~] 备份保留清理入口已具备，定时备份可自动保留最近 14 个备份目录，仍需真实机验证。
- [ ] 配置 HTTPS 域名和反向代理。
- [~] 提供 `scripts/server-health.sh` 服务器健康诊断脚本，仍需接入正式监控告警。
- [~] 提供 `scripts/server-upgrade.sh` 升级回滚脚本，包含升级前备份、拉取版本、重建、健康检查和失败回滚；仍需真实机演练。

## 2. 安全加固

- [x] 部署脚本会生成随机 `DB_PASSWORD`、`REDIS_PASSWORD`、`JWT_SECRET`、`MINIO_PASSWORD`。
- [x] 后端生产启动会拦截明显不安全的默认密钥。
- [x] 项目 ID 请求头增加格式校验，避免非法项目 ID 引发 500。
- [~] 管理员密码重置脚本已具备，生产部署后必须立即修改默认密码。
- [ ] 配置 HTTPS 后，将所有客户端 API 地址统一切换为 `https://域名/v1`。
- [ ] 检查 MinIO 文件访问权限，确认附件只能通过后端鉴权访问。
- [ ] 配置上传文件大小限制和允许的文件类型。
- [ ] 检查 `.env` 不进入 Git，不在截图或日志中泄漏。
- [ ] 根据真实组织结构复核管理员、工程师、巡检员、查看员权限矩阵。

## 3. Web 管理端验收

### 登录与项目

- [x] Web 可使用 `/v1` 作为同源 API 地址登录。
- [x] 项目下拉可自动切换到有效项目 ID，避免旧缓存项目 ID 造成请求失败。
- [~] 项目管理页面已具备基础新增/编辑能力。
- [ ] 验证管理员创建多个项目后，项目切换不会串数据。
- [ ] 验证非管理员只能访问授权项目。

### 工单闭环

- [x] 工单号已改为事务内日序列表生成，避免并发冲突和删除后回退。
- [x] 工单列表分页排序 500 已修复。
- [~] 工单创建、派单、接单、维修记录、提交验收、验收通过/退回接口已具备。
- [ ] 按真实场景完整跑通：报修 -> 派单 -> 接单 -> 添加维修记录 -> 扣减备件 -> 提交验收 -> 验收归档。
- [ ] 验证工程师不能操作非本人负责的工单。
- [ ] 验证管理员可取消、挂起、恢复、验收工单。
- [ ] 验证工单详情中的设备、报修人、负责人、维修记录显示正确。
- [ ] 验证空列表、失败提示、状态按钮禁用逻辑。

### 维修台账

- [x] 维修台账依赖的工单列表 500 已修复。
- [~] 台账列表、维修过程查看、Excel 导出入口已具备。
- [ ] 验证按关键字搜索工单号、设备、故障描述。
- [ ] 验证维修记录中的配件、外协费用、工程师显示正确。
- [ ] 验证导出的 Excel 内容和中文列名正确。

### 设备台账

- [x] 设备列表排序路径已修复，避免同类 TypeORM 分页/排序问题。
- [x] 项目内设备编号和二维码唯一约束已具备。
- [~] 设备新增、编辑、删除、扫码查询、详情联动已具备。
- [ ] 验证设备创建时必填字段和重复编号提示。
- [ ] 验证设备详情可查看维修历史并发起报修。
- [ ] 验证批量导入格式和错误报告。

### 备件库存

- [x] 入库/出库使用数据库原子更新。
- [x] 工单维修记录和备件扣减放入同一事务。
- [~] 备件增删改查、入库、出库、日志、低库存预警已具备。
- [ ] 验证库存不足时不会产生部分扣减。
- [ ] 验证多个用户同时出库时库存不会丢失更新。
- [ ] 验证低库存预警展示和数量边界。

### 巡检管理

- [x] 巡检记录分页排序 500 已修复。
- [~] 巡检计划、今日巡检、巡检记录、异常自动生成工单已具备。
- [ ] 验证新建每日/每周/每月巡检计划。
- [ ] 验证巡检记录提交后下一次巡检时间计算正确。
- [ ] 验证异常巡检自动生成工单，并能进入工单闭环。
- [ ] 验证巡检员权限和管理员停用计划逻辑。

### 报表与数据

- [x] 报表按月回退日期已修复，规避 JavaScript 月末溢出。
- [x] 报表 SQL 已兼容旧库 uuid/varchar 关联问题。
- [~] 运营汇总、周趋势、设备状态、备件排行、Excel 导出、项目 JSON 备份/恢复预检已具备。
- [ ] 验证所有报表在无数据、有数据、多项目场景下不报错。
- [ ] 验证 Excel 导出文件可打开、数据完整。
- [ ] 验证 JSON 备份恢复预检和恢复流程。

### 用户权限

- [~] 用户新增、编辑、禁用、角色、项目授权已具备。
- [ ] 验证管理员、工程师、巡检员、查看员菜单和接口权限。
- [ ] 验证禁用用户无法登录。
- [ ] 验证工程师工作量统计。

### 工具箱

- [~] DMX、功率、BPM、LTC、光束角、照度、RGB/色温、故障诊断、MA 宏、术语、灯库、灯位、理论工具已具备基础页面。
- [ ] 验证每个工具输入边界、空值、极值和移动端显示。
- [ ] BPM 音频文件自动识别尚未作为生产功能交付。
- [ ] LTC 生成结果需用真实控台或音频工具验证兼容性。

## 4. 手机 APP 验收

- [x] Android 打包脚本已具备。
- [x] Android 内测 APK 可发布到 `deploy/downloads/`。
- [~] React Native 手机端已接入登录、首页、工单、设备、备件、巡检、通知、工具箱等主要入口。
- [ ] Android 真机安装测试。
- [ ] Android 登录真实服务器测试。
- [ ] Android 扫码、拍照上传、附件访问权限测试。
- [ ] Android 弱网、断网、重新登录测试。
- [ ] iOS 在 macOS + Xcode + Apple Developer 环境完成签名打包。
- [ ] iOS 真机/TestFlight 登录测试。
- [ ] 手机端离线工具箱逐项验收。

## 5. 桌面客户端验收

- [x] Electron 桌面端打包脚本已具备。
- [x] Windows 安装包本地生成能力已验证。
- [~] Mac/Linux 打包入口已具备，但需要目标平台构建。
- [ ] Windows 安装包在干净机器安装、登录、卸载测试。
- [ ] Windows 生产代码签名。
- [ ] macOS DMG 打包、签名和公证。
- [ ] macOS 安装、登录、自动阻止提示处理。
- [ ] Linux AppImage 运行测试。
- [ ] 桌面端 API 地址配置、退出登录、缓存清理测试。

## 6. 自动化测试与质量

- [x] 后端和 Web 构建可通过。
- [~] 工具箱核心库已有部分单元测试。
- [~] 增加后端核心回归测试：已覆盖项目权限守卫、工单分页排序、维修记录事务扣件、备件库存失败回滚、巡检异常生成工单、报表 Excel 导出、用户项目隔离、角色过滤、工作量统计、用户接口项目 ID 校验和主要菜单 API 路由映射；仍需补完整 HTTP 接口测试。
- [ ] 增加 Web 冒烟测试：登录、项目切换、工单列表、巡检列表、报表页。
- [ ] 增加数据库迁移测试，覆盖旧库升级到最新版本。
- [ ] 增加客户端打包后的安装包校验说明。
- [ ] 建立发布版本号和变更日志。

## 7. 下一步执行顺序

1. 以服务器真实环境为准，逐菜单执行 Web 管理端验收。
2. 修复验收中出现的页面 bug、接口 500、权限问题和数据错位。
3. 完成生产备份/恢复演练，并配置定时备份。
4. 配置域名 HTTPS，把客户端 API 地址统一迁移到 HTTPS。
5. Android 真机安装测试，Windows 干净机器安装测试。
6. 准备 macOS/iOS 签名环境，生成可安装包。
7. 补接口自动化测试和 Web 冒烟测试。
8. 发布第一个内测版本号，例如 `v0.9.0-internal`。

## 8. 本次阶段记录

- 2026-06-07：修复 PostgreSQL uuid/varchar 关联导致的报表、工单、巡检 500。
- 2026-06-07：修复 TypeORM `getManyAndCount()` 分页排序触发的 `databaseName` 异常。
- 2026-06-07：重写 README 为正式生产入口文档，新增本生产验收清单。
- 2026-06-07：新增后端核心业务回归测试，覆盖工单、备件、巡检和项目权限守卫，共 4 个测试套件 13 个用例。
- 2026-06-07：补充报表导出和用户权限回归测试，并为用户列表接口增加项目 ID 格式校验，后端回归测试扩展到 7 个测试套件 21 个用例。
- 2026-06-07：加固服务器备份恢复脚本，新增备份列表、SHA256 校验、恢复确认、恢复前停止写入服务和旧备份清理入口。
- 2026-06-07：补充定时备份安装/移除入口和服务器健康诊断脚本，README 同步新增运维操作命令。
- 2026-06-07：新增服务器升级回滚脚本，升级前自动备份，升级后检查 `/v1/health`，失败时尝试回滚应用代码。
- 2026-06-07：新增后端主要菜单 API 路由映射冒烟测试，后端回归测试扩展到 8 个测试套件 26 个用例。
